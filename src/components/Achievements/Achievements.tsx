import styles from "./achievements.module.css"

interface AchievementsProps {}

function Achievements(prop: AchievementsProps) {
  return <div className={styles.achievements}></div>
}

export default Achievements
